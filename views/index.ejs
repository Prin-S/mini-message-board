<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Home</title>
  </head>
  <body>
    <div>
      <% messages.forEach(message => { %>
        <p>
          <p><%= message.text %></p>
          <p><%= message.user %></p>
          <p><%= message.added.toLocaleString('en-GB') %></p>
        </p>
      <% }); %>
    </div>
  </body>
</html>